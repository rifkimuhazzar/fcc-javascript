const myConcat = (arr1, arr2) => arr1.concat(arr2); // .concat() untuk menggabungkan

console.log(myConcat([1, 2], [3, 4, 5]));

// LATIHAN, jika hanya 1 param bisa tanpa ()
const sayHello = (name) => console.log("Hello " + name);

sayHello("World");
